16. Create a logical vector and demonstrate the use of the any() and all() functions.



# Creating a logical vector
logical_vector <- c(TRUE, FALSE, TRUE, FALSE)

# Using any() function - checks if at least one element is TRUE
any_result <- any(logical_vector)
print(any_result)  # Returns TRUE because there's at least one TRUE in the vector

# Using all() function - checks if all elements are TRUE
all_result <- all(logical_vector)
print(all_result)  # Returns FALSE because not all elements are TRUE

# Scenario explanation:
# In this case, any() returns TRUE because there is at least one TRUE value in the vector.
# all() returns FALSE because not all elements are TRUE; some are FALSE.
